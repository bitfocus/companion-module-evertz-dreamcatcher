{
  "name": "companion-module-evertz-dreamcatcher",
  "version": "1.0.9",
  "description": "Evertz DreamCatcher Companion module",
  "license": "MIT",
  "main": "pkg/main.js",
  "scripts": {
    "build": "tsc && mkdir -p pkg && cp -R dist/* pkg/ && cp -R companion pkg/ && cp README.md LICENSE pkg/",
    "companion-module-build": "yarn build"
  },
  "files": [
    "pkg",
    "companion",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@companion-module/base": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
